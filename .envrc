# Konductor direnv configuration
# Uses nix-direnv for flake integration with caching

# Airgap mode: use --offline when running from /opt/konductor (VM pre-cached)
if [[ "$PWD" == "/opt/konductor" ]]; then
  use flake .#konductor --offline
else
  use flake .#full
fi

# Add project bin wrappers to PATH (after flake loads)
PATH_add .config/bin

# Load .env file for project-specific variables
dotenv_if_exists .env
