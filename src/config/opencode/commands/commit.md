---
description: Stage and commit changes using conventional commits ceremony
---

# STOP - READ THIS FIRST

**BEFORE YOU DO ANYTHING ELSE:**

You are about to execute a commit ceremony. The #1 rule is:

## NEVER TRUNCATE OUTPUT

```
FORBIDDEN: | head
FORBIDDEN: | tail
FORBIDDEN: | head -N
FORBIDDEN: | tail -N
FORBIDDEN: > file.txt
FORBIDDEN: --stat (alone)
FORBIDDEN: --oneline
```

If you use ANY of these patterns, you have FAILED the ceremony.

---

Now load the `git-commit` skill and execute all steps of the commit ceremony.

$ARGUMENTS
