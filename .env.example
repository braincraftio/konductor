# Konductor Environment Configuration
# Copy to .env or .env.local and customize as needed
#
# Load order: .env.local (if exists) -> .env
# .env.local is gitignored for local overrides

# =============================================================================
# QCOW2 Build Configuration
# =============================================================================

# libguestfs backend (direct = no libvirt dependency)
LIBGUESTFS_BACKEND=direct

# Output filename (default includes date stamp)
# Override to use a fixed name for reproducible builds
# QCOW2_OUTPUT=konductor.qcow2

# Temporary directories for build process
QCOW2_MOUNT=/tmp/nixmount
QCOW2_CLOUD_INIT_DIR=/tmp/konductor-build-cloud-init
QCOW2_PIDFILE=/tmp/konductor-build-vm.pid
QCOW2_SSH_KEY_DIR=/tmp/konductor-build-ssh
QCOW2_LOGFILE=/tmp/konductor-build-vm.log

# VM configuration
QCOW2_VM_MEMORY=8192
QCOW2_SSH_PORT=2222
QCOW2_SSH_TIMEOUT=300

# =============================================================================
# Container Image Configuration
# =============================================================================

# Container registry and image name
CONTAINER_REGISTRY=docker.io
CONTAINER_IMAGE=containercraft/konductor
CONTAINER_TAG=qcow2

# =============================================================================
# Development Configuration
# =============================================================================

# Log level for verbose output
# LOG_LEVEL=debug

# Skip certain build phases (for development)
# SKIP_NIX_BUILD=false
# SKIP_VM_PHASE=false
# SKIP_COMPRESS=false
