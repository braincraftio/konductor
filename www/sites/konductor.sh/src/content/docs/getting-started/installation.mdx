---
title: "Installation"
description: "Setting up Lix and initializing a Konductor project."
section: "Getting Started"
order: 2
---

import { Card, CardGrid, Note } from '../../../components/docs/MDXComponents';

<Note type="info" title="Prerequisites">
  Konductor requires a Nix installation with Flakes enabled. We explicitly recommend **Lix**, a robust Nix variant, for the best experience.
</Note>

## 1. Install Lix

To install Lix with flakes enabled and trusted user configuration (required for binary caches):

```bash
curl -sSf -L https://install.lix.systems/lix | sh -s -- install \
  --no-confirm \
  --extra-conf "experimental-features = nix-command flakes" \
  --extra-conf "warn-dirty = false" \
  --extra-conf "accept-flake-config = true" \
  --extra-conf "trusted-users = root @wheel @admin $(whoami)"
```

Add the Nix daemon to your shell profile (e.g., `~/.bashrc` or `~/.zshrc`):

```bash
if [ -e /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh ]; then
  . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh
fi
```

## 2. Quick Start

Choose your preferred way to interact with Konductor.

<CardGrid>
  <Card title="Use the Registry (Recommended)" icon="🚀">
    Add Konductor to your Nix registry for instant access from any directory without cloning the repo.

    <div className="mt-4">
    ```bash
    nix registry add konductor github:braincraftio/konductor
    nix develop konductor#full
    ```
    </div>
  </Card>
  <Card title="Task Automation" icon="🤖">
    If you prefer `mise` over raw Nix commands, install it to run simplified tasks like `mise run lint`.

    <div className="mt-4">
    ```bash
    curl https://mise.run | sh
    # Add to shell:
    echo 'eval "$(~/.local/bin/mise activate bash)"' >> ~/.bashrc
    ```
    </div>
  </Card>
</CardGrid>

## 3. Local VM Development

The canonical way to develop Konductor itself is using a local NixOS VM with your repository mounted.

<div className="my-8 p-6 bg-surface-paper border border-border-default rounded-xl">
### 1. Clone & Setup
```bash
gh repo clone braincraftio/konductor ~/Git/github.com/braincraftio/konductor
ln -s ~/Git/github.com/braincraftio/konductor ~/.konductor
cd ~/.konductor
```

### 2. Deploy VM
```bash
nix develop .#konductor
runme run nix-build-qcow2-deploy
```

### 3. Connect
```bash
ssh localhost
cd /workspace && nix develop .#konductor
```
</div>
